<%= form_for(:user, :as => "user", :url => confirmation_path(:user) ) do |f| %>
    <%= f.email_field :email, :placeholder => User.human_attribute_name(:email), :class => "input-medium" %>
    <%= f.submit "이메일 등록하기", :class => "btn btn-small" %>
<% end %>